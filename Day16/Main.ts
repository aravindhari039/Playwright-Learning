/*
import { appname } from "./Module";
import { add } from "./Module";
import { Example } from "./Module"; 
 */

import { appname, add, Example } from "./Module"; 



console.log(appname);
console.log(add(10,20));
console.log(Example.toUpper("welcome"))  // in class must to make it as static of methods only work here